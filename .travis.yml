language: php
php:
  - 7.0
  - 7.1
  - 7.2
sudo: false
install:
  - composer install
before_script:
  - pushd public
  - ../vendor/bin/aerys -d -c index.php &
  - popd
  - sleep 4
script:
  - DB=config vendor/bin/phpunit

